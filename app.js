import Map from './src/components/map-explorer/Map/Map.js';

// Assuming the Map class is properly exported and can be imported here
document.addEventListener('DOMContentLoaded', () => {
    // Initialize the Map component when the DOM is fully loaded
    const mapComponent = new Map();
});